# Copy to .env.local and fill in. Get a free key at https://aistudio.google.com/apikey
# In production (Vercel), set this in Project Settings → Environment Variables.
GEMINI_API_KEY=your-google-gemini-api-key

# Optional: override the model (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
